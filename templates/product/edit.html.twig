{% include 'default/_form.html.twig' with {
    page_title: 'New product',
    attr: {
        method: 'PUT',
        action: url('product_edit', {'id': type_id})
    },
    back_to_list_path: url('product_view', {'id': type_id}),
} %}
