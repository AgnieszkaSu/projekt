{% include 'default/_form.html.twig' with {
    page_title: 'New product',
    attr: {
        method: 'POST',
        action: url('product_new_with_id', {'id': id})
    },
    back_to_list_path: url('product_view', {'id': id}),
} %}